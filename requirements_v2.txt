# Resume Extractor v2 — Python dependencies
# Install: pip install -r requirements_v2.txt
# Then:    python -m spacy download en_core_web_sm   (needed by the v1 extractors v2 reuses)

# API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.5
requests>=2.31

# Database (SQLAlchemy = sqlite / AWS RDS Postgres / AWS RDS MySQL; pymongo = Atlas / DocumentDB)
sqlalchemy>=2.0
psycopg2-binary>=2.9
pymysql>=1.1
pymongo>=4.6

# Text extraction
pdfminer.six>=20231228
python-docx>=1.1
docx2txt>=0.8
striprtf>=0.0.26
pillow>=10.0
pdf2image>=1.17
pytesseract>=0.3.10

# Extraction helpers (also used by the v1 extractors under app/)
phonenumbers>=8.13
rapidfuzz>=3.6
spacy>=3.7

# Optional — better OCR for scanned resumes (large download, needs torch):
# easyocr>=1.7

# Tests
pytest>=8.0
httpx>=0.27
